import { EventEmitter2 as EventManager } from "eventemitter2"

class AppEventManager extends EventManager {
    constructor(evMap: EventListenerMap) {
        super();
        // register events
    }
}